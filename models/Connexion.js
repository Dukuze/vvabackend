const mongoose = require('mongoose');

const conb = mongoose.Schema({
    titre: {type : 'String'},
    username : {type : 'String'},
    imagename : {type : 'String'},
    
})

module.exports = mongoose.model('', vva)